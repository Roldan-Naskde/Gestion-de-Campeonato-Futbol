services:
  - type: web
    name: django-backend
    env: python
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn backend.asgi:application --host=0.0.0.0 --port=8000